import React, { Component } from 'react';

function A {

    const Messages = () => {
        const { username } = useContext(AppContext)
        render() {
            return (
                <div>
                    this is A
                </div>
            );
        }
    }

    export default A;